import React from "react";

const CartPage = () => {
    return (
        <div className={"text-center"}>
            <p>There should have been a table here, but I moved it to the main page.</p>
            <p>Click on the logo <b>( ReStore )</b> to go back.</p>
        </div>
    )
};

export default CartPage